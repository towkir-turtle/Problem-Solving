const randomNumber = Math.round(Math.random() * 10);
// console.log(randomNumber);

// Math.random() * (highestNumber - lowestNumber) + lowestNumber
const random = Math.round(Math.random(10 - 1 + 1) * 10);
console.log(random);
